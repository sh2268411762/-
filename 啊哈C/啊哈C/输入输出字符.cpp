/*
#include <stdio.h>
#include <stdlib.h>
int main()
{
    char a;
    char b;
    char c;
    char d;

    //scanf("%c",&a);          
    //printf("%c",a);

    //b=getchar();            //getchar()读取一个字符，输入后等待用户按“enter”键结束（带回显）
    //printf("%c",b);

    //c=getche();             //getche()读取一个字符，输入后立即获取字符，不用按“Enter”键结束（带回显）
    //printf("%c",c);

    //d=getch();                ////getch()读取一个字符，输入后立即获取字符，不用按“Enter”键结束（不带回显）
   // printf("%c",d);
    return 0;
}
*/